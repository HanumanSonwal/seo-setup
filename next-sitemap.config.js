/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://seo-setup.vercel.app', // Replace with your actual domain
    generateRobotsTxt: true, // (Optional) Generate robots.txt
    sitemapSize: 5000, // Adjust the limit of URLs per sitemap (if needed)
    generateIndexSitemap: true, // Create an index sitemap
  };
  